# 本地开发环境配置
# 启动命令：java -jar yumy-bootstrap.jar --spring.profiles.active=local

spring:
  # 数据源
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://192.168.110.112:15432/yumy?search_path=admin,toc,public
    username: yumy
    password: yumy123
  # Redis
  data:
    redis:
      host: 192.168.110.112
      port: 6379
      password: yumy123

# JWT
jwt:
  secret: ${JWT_SECRET:yumy-boot-jwt-secret-key-must-be-at-least-32-characters-long}

# CORS 配置（本地环境允许所有源）
cors:
  allowed-origins: "*"
